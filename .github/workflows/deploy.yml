# CONSOLIDATED — this workflow has been merged into ci.yaml.
#
# deploy.yml used the dagster-io/dagster-cloud-action PEX/Docker template, which:
#   - auto-detects PEX vs Docker and can fall through to the PEX builder
#   - ignores the project's custom Dockerfile and uv-based layer cache
#   - ran a second, parallel deploy on every main push alongside ci.yaml
#
# The ci.yaml deploy job now handles everything: it builds the Docker image
# explicitly (with uv + libgomp1 + layer caching) and registers it with
# Dagster Cloud via dagster-cloud-cli ci init / set-build-output / deploy.
#
# Set to workflow_dispatch only so it never auto-triggers.
name: "⚠ Disabled — see ci.yaml"
on:
  workflow_dispatch:

jobs:
  notice:
    runs-on: ubuntu-22.04
    steps:
      - run: echo "This workflow is disabled. All CI/CD runs through ci.yaml."
